import styled from 'styled-components';

const Container = styled.header`
  background-color: red;
`;

const CartStyled = styled.div`
  display: flex;
  justify-content: flex-end;
`;
const Value = styled.span`
  margin: 0px 24px;
  color: white;
  font-size: 20px;
`;

export { Container, CartStyled, Value };
